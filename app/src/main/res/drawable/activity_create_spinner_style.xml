<!-- res/drawable/spinner_button_style.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Gleiche Hintergrundfarbe wie der Button -->
    <solid android:color="@color/surfaceVariantColor" />

    <!-- rundere Ecken -->
    <corners android:radius="28dp" /> <!-- <- Hier ändern wir’s -->

    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>
